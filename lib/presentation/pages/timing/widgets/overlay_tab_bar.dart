import 'package:flutter/material.dart';

class OverlayTabBar extends StatelessWidget {
  const OverlayTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    // Step1：默认不显示、不占位
    return const SizedBox.shrink();
  }
}
