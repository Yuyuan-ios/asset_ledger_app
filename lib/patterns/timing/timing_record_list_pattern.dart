import 'package:flutter/material.dart';

class TimingRecordListPattern extends StatelessWidget {
  const TimingRecordListPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
