import 'package:flutter/material.dart';

class TimingSummaryPattern extends StatelessWidget {
  const TimingSummaryPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
