import 'package:flutter/material.dart';

class LedgerSummaryPattern extends StatelessWidget {
  const LedgerSummaryPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
