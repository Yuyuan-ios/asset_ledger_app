import 'package:flutter/material.dart';

class LedgerListPattern extends StatelessWidget {
  const LedgerListPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
