import 'package:flutter/material.dart';

class LedgerRecordPattern extends StatelessWidget {
  const LedgerRecordPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
