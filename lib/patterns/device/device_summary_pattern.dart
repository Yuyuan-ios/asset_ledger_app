import 'package:flutter/material.dart';

class DeviceSummaryPattern extends StatelessWidget {
  const DeviceSummaryPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
