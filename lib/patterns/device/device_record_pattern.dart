import 'package:flutter/material.dart';

class DeviceRecordPattern extends StatelessWidget {
  const DeviceRecordPattern({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
