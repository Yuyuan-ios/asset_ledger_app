This directory is reserved for future Dart-aware ast-grep rules.

The `ast-grep 0.41.0` binary available in this workspace does not support Dart,
so the active boundary checks currently live in `tools/check_architecture.sh`.
