# 问题标题
2026-02-28 19:33，单设备项目仍显示批量修改入口

# 问题描述
项目详情弹窗在仅有一个设备时仍显示“批量修改”，会给用户造成可执行批量操作的误导，增加理解成本。

# 根本原因分析
设备单价区的右侧操作按钮没有根据设备数量做条件控制，默认始终展示。

# 解决方案与步骤
1. 在项目详情内容中增加 `canBatchEditRate` 条件。
2. 仅当设备数量大于 1 时，显示“批量修改”按钮。
3. 单设备场景下只保留“设备单价”标题，去掉无意义操作入口。

# 改进启示
操作入口应与实际可执行范围保持一致；当动作在当前场景无价值时，直接隐藏比置灰更清晰。
